apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnra-mvp.fullname" . }}
  labels:
    {{- include "cnra-mvp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cnra-mvp.selectorLabels" . | nindent 4 }}
